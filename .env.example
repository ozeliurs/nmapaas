API_KEY=development-key
# Comma-separated scan locations. Each entry is `name` (scan directly, no
# VPN) or `name:subnet-prefix` (WireGuard namespace on <subnet>.0/24).
# Each VPN-backed location needs a matching ./wireguard/<name>.conf.
LOCATIONS=de-ber:10.200.1,de-fra:10.200.2,jp-tok:10.200.3
